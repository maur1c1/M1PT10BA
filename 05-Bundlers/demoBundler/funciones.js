//const {num1, num2, nomme} = require("./constantes")
//import {num1, num2, nomme} from "./constantes"
import alumno from "./constantes";
//destructuring

let arr = [1,2,3];
let [a,b,c]=arr; 
export const sum = (a,b) => a+b;

export const printName = (nam) =>{
    console.log(nam);
}

