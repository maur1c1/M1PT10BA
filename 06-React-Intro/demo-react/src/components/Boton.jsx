//funciones

const Boton = (props) =>{
    // const {boton} = props; we can use destructuring too
    return(
        <>
            <button>{props.text}</button>
        </>
    )
}

export default Boton;